# Supply-chain advisory policy for kzktdk.
#
# Each `ignore` below is an ACCEPTED RISK documented in the supply-chain
# audit, NOT a blind suppression. Re-review on the date noted, or when the
# dependency chain changes (e.g. inpaint replacement, font-stack migration).
[advisories]
ignore = [
    # RUSTSEC-2024-0436 (paste 1.0.15, unmaintained): transitive via
    # image -> exr -> pulp. Stable proc-macro with no replacement;
    # accepted risk. Permanent, re-check only if pulp drops paste.
    "RUSTSEC-2024-0436",
    # RUSTSEC-2026-0192 (ttf-parser 0.25.1, unmaintained): via ab_glyph
    # 0.2.32 (direct dep; no ab_glyph release escapes it yet). Accepted
    # TEMPORARILY. REVIEW BY 2026-12-10: migrate font stack
    # (swash/cosmic-text) if still unmaintained.
    "RUSTSEC-2026-0192",
]
